# 빙고 게임

3x3 빙고 게임을 만들고 참여할 수 있는 웹 애플리케이션입니다.

## 설정

### 1. Supabase

1. [Supabase](https://supabase.com)에서 프로젝트를 생성합니다.
2. SQL Editor에서 아래 **전체 테이블 생성 스크립트**를 실행합니다.

```sql
-- ============================================================
-- 빙고 게임 전체 테이블 생성 스크립트
-- ============================================================

-- 1. 멤버
create table bingo_members (
  id bigint primary key generated by default as identity,
  name text not null,
  password text not null,
  created_at timestamptz default now()
);

-- 2. 게임 (3×3 고정, 승리 조건: 완성할 줄 수)
create table bingo_games (
  id bigint primary key generated by default as identity,
  title text not null,
  grid_size int not null default 3,
  win_lines int not null default 1,
  start_date date not null,
  end_date date not null,
  prize1 text,
  prize2 text,
  prize3 text,
  created_at timestamptz default now()
);

-- 3. 게임 격자 칸 (각 칸의 숫자·단어 등)
create table bingo_game_cells (
  id bigint primary key generated by default as identity,
  game_id bigint not null references bingo_games(id) on delete cascade,
  row_index int not null,
  col_index int not null,
  content text not null,
  unique(game_id, row_index, col_index)
);
create index idx_bingo_game_cells_game on bingo_game_cells(game_id);

-- 4. 멤버별 달성한 칸 (빙고판에서 클릭해 완료한 목표, 달성 시각 포함)
create table bingo_marked_cells (
  id bigint primary key generated by default as identity,
  game_id bigint not null references bingo_games(id) on delete cascade,
  member_id bigint not null references bingo_members(id) on delete cascade,
  row_index int not null,
  col_index int not null,
  marked_at timestamptz default now(),
  unique(game_id, member_id, row_index, col_index)
);
create index idx_bingo_marked_cells_game_member on bingo_marked_cells(game_id, member_id);

-- 5. 게임별 달성 기록 (멤버당 한 행, 완성한 줄 수 = bingo_count)
create table bingo_achievements (
  id bigint primary key generated by default as identity,
  game_id bigint not null references bingo_games(id) on delete cascade,
  member_id bigint not null references bingo_members(id) on delete cascade,
  bingo_count int not null default 0,
  updated_at timestamptz default now(),
  unique(game_id, member_id)
);
create index idx_bingo_achievements_game on bingo_achievements(game_id);
```

**이미 `bingo_marked_cells` 테이블을 사용 중인 경우** (달성 시각 컬럼 추가):

```sql
alter table bingo_marked_cells add column if not exists marked_at timestamptz default now();
```

3. `index.html` 상단의 `SUPABASE_URL`, `SUPABASE_ANON_KEY`에 프로젝트 URL과 anon public 키를 넣습니다.

### 2. 카카오톡 공유

1. [Kakao Developers](https://developers.kakao.com)에서 앱을 만들고 **JavaScript 키**를 발급받습니다.
2. 플랫폼에 웹 사이트 URL을 등록합니다.
3. `index.html`의 `KAKAO_JS_KEY`에 JavaScript 키를 넣습니다.

## 사용 방법

- **멤버 등록**: 이름과 비밀번호로 멤버를 등록합니다.
- **게임 등록**: 게임 이름, 승리 조건(완성할 줄 수), 3×3 격자의 칸 내용(가로 3×세로 3 입력란에 숫자·단어 등 9개 입력), 시작일·종료일, 1~3등 상품을 입력해 빙고 게임을 만듭니다. 격자 크기는 3×3으로 고정됩니다.
- **현황**: 게임을 선택하면 참여 멤버 순위(빙고 달성 개수, 동점 시 먼저 달성한 순)가 표시됩니다. 상위 3명에 🥇🥈🥉 메달이 표시됩니다.
- **참여하기**: 게임 선택 후 멤버를 선택하고 "참여하기"를 누르면 해당 게임에 참여합니다.
- **빙고 +1**: 순위 표에서 멤버별 "+1" 버튼으로 빙고 달성 수를 올립니다.
- **이력 조회**: 순위 옆 📋 링크로 해당 게임의 달성 이력 목록을 볼 수 있습니다.
- **카카오톡 공유**: 노란색(#fee500) 버튼으로 현재 순위/시간 정보를 카카오톡에 공유할 수 있습니다.

## 기술 스택

- HTML5, CSS3, Vanilla JavaScript
- Supabase (PostgreSQL, JS SDK v2)
- 카카오톡 JavaScript SDK
